# Hugging Face token — must have "Inference Providers" permission enabled.
# Generate one at https://huggingface.co/settings/tokens
HF_API_KEY=hf_your_token_here

# Server port (optional, defaults to 3000)
PORT=3000
